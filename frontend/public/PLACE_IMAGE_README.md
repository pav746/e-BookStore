Place the background image provided in the conversation here:

- Path (relative to project): `public/assets/hero-bg.jpg`

How to add:
1. Create the folder `frontend/public/assets` if it doesn't exist.
2. Save the attached image file as `hero-bg.jpg` into that folder.
3. Restart the dev server (if running) or reload the page.

Why: `App.css` and `Hero.jsx` reference `/assets/hero-bg.jpg` to render the site-wide warm book-photo background.
